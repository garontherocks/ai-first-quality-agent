# The deterministic provider is the default and requires no credentials.
AI_PROVIDER=mock

# To opt into a live run, use AI_PROVIDER=openai and provide both values locally.
# Never commit a real key.
OPENAI_API_KEY=
OPENAI_MODEL=

# Required by the server before any side-effecting tool can run.
TOOL_APPROVAL_TOKEN=
